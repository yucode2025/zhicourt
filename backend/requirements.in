# Direct runtime dependencies. Keep compatible ranges here; requirements.lock
# is the fully version-pinned CI/deployment constraint set.
alembic>=1.13,<2
bcrypt>=4.1,<6
cryptography>=42,<51
fastapi>=0.115,<1
greenlet>=3.1,<3.3
httpx>=0.27,<1
# httptools 0.8 has no CPython 3.10 Linux wheel; constrain uvicorn's optional
# accelerator until the project's Python baseline is raised.
httptools>=0.6.4,<0.7
pydantic>=2.7,<3
pymysql>=1.1,<2
python-dotenv>=1,<2
redis>=5,<9
sqlalchemy>=2,<3
uvicorn[standard]>=0.30,<1
